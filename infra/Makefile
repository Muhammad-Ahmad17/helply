# Ragify deploy helpers — delegates to deploy/Makefile
# Usage: make -C infra deploy

.PHONY: deploy deploy-do deploy-oci verify status

deploy deploy-do deploy-oci verify status:
	$(MAKE) -C ../deploy $@
